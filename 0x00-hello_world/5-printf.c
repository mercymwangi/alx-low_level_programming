#include <stdio.h>
/**
 * main - Entry point
 * Description: The return value of main is 0.
 * Return 0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
