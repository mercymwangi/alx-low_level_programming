#include <stdio.h>
/**
 * main - print hexadecimals
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;
	char l;

	n = 0;
	l = 'a';
	while
		(n < 10); {
			putchar(n + '0');
			n++;
		}
	while
		(l <= 'f'); {
			putchar(l);
			l++;
		}
	putchar('\n');
	return (0);
}
